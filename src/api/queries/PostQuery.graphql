query PostQuery($id: ID!) {
  postDetail(id: $id) {
    id
    name
    text
    image
    datePublished
    dateModified
    category {
      name
    }
    keywords {
      name
    }
    writtenBy {
      username
    }
  }
}
